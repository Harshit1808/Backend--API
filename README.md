__To re-create the Backend__
1. run `npm install`
2. create a `.env` file
3. add following fields:

     PORT = `/// YOUR PORT NUMBER////`
     
     DBURI = `/// YOUR DB URI////`
     
     SECRET = `/// A SECRET////`

4. run `npm start`